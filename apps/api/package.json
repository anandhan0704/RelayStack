{
  "name": "@relaystack/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "node dist/server.js",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "@fastify/helmet": "^12.0.1",
    "@relaystack/config": "0.1.0",
    "@relaystack/domain": "0.1.0",
    "@relaystack/shared": "0.1.0",
    "fastify": "^5.1.0",
    "pino": "^9.5.0",
    "zod": "^3.24.1"
  }
}

